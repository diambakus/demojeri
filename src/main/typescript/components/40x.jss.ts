import { makeStyles } from '@material-ui/core'

export default makeStyles({
    errorPage: {
        textAlign: 'center' as 'center',
        color: 'rgb(84, 86, 91)'
    },
    icon: {
        fontSize: 144
    }
})
